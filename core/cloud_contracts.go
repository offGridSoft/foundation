package core

const (
	// OffgridGoogleCloudProjectID is the shared Google Cloud ownership boundary
	// for OffGrid products. Product-specific resources remain product-owned.
	OffgridGoogleCloudProjectID = "offgridsoftware"
	// DefaultFirestoreDatabaseID is Google's compiler-visible identifier for a
	// project's default Firestore database.
	DefaultFirestoreDatabaseID = "(default)"
)
